# Optional: backend origin for Vite dev proxy
# If your Spring Boot app runs on another host/port, change this value.
VITE_BACKEND_TARGET=http://localhost:8080

# Optional: override Axios base path.
# Leave as /api for same-origin/proxy behavior.
VITE_API_BASE_URL=/api
